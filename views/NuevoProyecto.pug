extends layout
include mixins/CrearProyecto

block contenido 
    .contenido-principal 
        h1=nombrePagina

        //- pre= vardump(proyecto)

        if errores 
            each error in errores
                .alerta= error.texto
                //el nombre de el mixins
        +crearProyecto(proyecto)