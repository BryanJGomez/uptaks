doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1= nombrePagina 
            p Agrega tu E-mail para restablecer tu contraseña
            
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            form.caja-login(method="POST" action="/restablecer")
                .campo
                    label(for="Email") E-mail
                    input(type="text" name="email" value=email placeholder="Tu E-mail")
                .campo.enviar
                    input.boton(type="submit" value="restablecer password")
                .campo.acciones
                a(href="/iniciar-sesion") Volver a la Pagina principal 